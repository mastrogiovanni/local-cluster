apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: htpasswd
  labels:
    app.kubernetes.io/name: htpasswd
rules:
  - apiGroups: [""]
    resources: ["secrets"]
    verbs: ["get", "list", "watch"]
